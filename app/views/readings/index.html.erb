<div class="container">
<h1>All Readings</h1>
<hr/>
<div class="center">
<%= link_to "Enter New Meter Readings", new_reading_path, class: "center btn btn-primary btn-white-text" %>
<br><br>
<%= will_paginate %>
</div>
<% @readings.each do |reading| %>
    
    <div class="row well">
        <div class="col-md-1">
            <h4><%= link_to reading.user.customer_number, user_path(reading.user) %></h4>
        </div>
        <div class="col-md-5">
            <h4><%= reading.user.company %></h4>
        </div>
        <div class="col-md-3">
            <h4>Equipment ID Number: <%= link_to reading.equipment_number, reading_path(reading) %></h4>
            <small>Created <%= time_ago_in_words(reading.created_at) %> ago</small><br>
            <small>Updated <%= time_ago_in_words(reading.updated_at) %> ago</small>
        </div>
        <div class="col-md-3">
            <% if logged_in? and current_user.admin? %>
                <p class="recorded">Recorded in System:</p>
                <% if reading.recorded == false %>
                   <p>No</p>
                <% else %>
                   <p>Yes</p>
                <% end %>
            <% end %>
        </div>
    </div>
    
<% end %>
<div class="center">
<%= will_paginate %>
</div>
</div>
